const VAPID_PUBLIC_KEY = "VAPID_PUBLIC_KEY";

const VAPID_PRIVATE_KEY = "VAPID_PRIVATE_KEY";

const VAPID_SUBJECT = "mailto:test@gmail.com";

module.exports = {
  VAPID_PUBLIC_KEY,
  VAPID_PRIVATE_KEY,
  VAPID_SUBJECT,
};
